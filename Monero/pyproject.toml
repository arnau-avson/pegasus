# This project was generated with 0.3.26 using template: https://github.com/beeware/briefcase-template @ v0.3.26
[tool.briefcase]
project_name = "monero"
bundle = "com.example"
version = "0.0.1"
url = "https://example.com/Monero"
license.file = "LICENSE"
author = "arnau-avson"
author_email = "arnau@avson.eu"

[tool.briefcase.app.Monero]
formal_name = "Monero"
description = "monero"
long_description = """More details about the app should go here.
"""
sources = [
    "src/Monero",
]
test_sources = [
    "tests",
]

requires = [
    # Add your cross-platform app requirements here
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.Monero.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.Monero.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.Monero.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.Monero.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.Monero.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.Monero.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.Monero.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "25.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.Monero.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.Monero.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.Monero.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.Monero.web]
requires = [
    # Add your web-specific app requirements here
]

